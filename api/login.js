const {bin,bcrypt,sid,token,publicUser}=require("./_lib");module.exports=async(req,res)=>{try{if(req.method!=="POST")return res.status(405).end();const{login,password}=req.body,db=await bin();const u=(db.users||[]).find(x=>x.username.toLowerCase()===String(login||"").toLowerCase()||x.email.toLowerCase()===String(login||"").toLowerCase());if(!u||u.status==="suspended"||!(await bcrypt.compare(password||"",u.passwordHash)))return res.status(401).json({error:u?.status==="suspended"?"Akun sedang dinonaktifkan.":"Username/email atau password salah."});res.setHeader("Set-Cookie",sid(token(u.id)));res.json({user:publicUser(u)})}catch(e){res.status(500).json({error:e.message})}};